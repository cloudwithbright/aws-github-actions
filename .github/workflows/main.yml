name: Github Action Tutorials
on:
  push:
    branches:
      - master
      - main
jobs:
  Development:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app01
    steps:
      - id: "A"
        name: Checkout Repository
        uses: actions/checkout@v4
      - id: "B"
        name: Context tutorials
        env:
          CUSTOM_GITHUB: ${{ toJson(github) }}
        run: echo $CUSTOM_GITHUB
